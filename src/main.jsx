import React from "react";
import ReactDOM from "react-dom/client";
import NutriXApp from "../NutriX.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NutriXApp />
  </React.StrictMode>
);
